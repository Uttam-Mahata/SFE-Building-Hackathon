jdk:
  - openjdk17 # Retaining openjdk17 as it was already there, though instruction mentioned 11.
              # If 11 is a strict requirement, this could be changed.
before_install:
  - ./gradlew clean
install:
  - ./gradlew :csfe:build :csfe:publishToMavenLocal :backend-sdk:build :backend-sdk:publishToMavenLocal
